@inherits LayoutComponentBase

<NavMenu />

<main class="container-fluid py-4">
    <div class="container">
        @Body
    </div>
</main>

<footer class="footer mt-auto">
    <div class="container">
        <div class="row">
            <div class="col-12 text-center">
                <p class="mb-0">
                    <strong class="text-white">Take Off Video Application @_appVersion</strong>
                    <span class="mx-2">|</span>
                    <span>by <a href="#" class="text-decoration-none text-primary">RG Progr</a></span>
                </p>
            </div>
        </div>
    </div>
</footer>


@code
{

    //string? _version;

    string? _appVersion;

    protected override void OnInitialized()
    {
        base.OnInitialized();

        //_appVersion = typeof(Program).Assembly.GetName().Version;

        _appVersion = GetType().Assembly.GetName().Version.ToString();

        //var _version = Environment.Version.ToString();
    }
}